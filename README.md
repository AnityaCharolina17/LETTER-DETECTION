# LETTER-DETECTION

Aplikasi Deteksi Huruf dan Kata.

This is a code bundle for Letter Detection App UI. The original project is available at https://www.figma.com/design/t5oqu6IwCHPsFrgS26iZ1l/Letter-Detection-App-UI.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
